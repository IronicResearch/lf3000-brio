#!/bin/bash

# Build FreeType libs for ARM
./configure --host=arm-linux --build=x86-linux --prefix=$ROOTFS_PATH/usr/local --enable-shared=yes
make CFLAGS="-c -Wall -O4 -mcpu=arm926ej-s"
cp -a objs/.libs/libfreetype.so* ../../Libs/arm/

