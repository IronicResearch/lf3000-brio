--- opengles_lite_gcc_source/Makefile_LF	2007-10-16 14:38:36.000000000 -0700
+++ opengles_lite_gcc_source/Makefile.arm	2010-02-17 17:29:18.000000000 -0800
@@ -5,17 +5,17 @@
 TARGETNAME = opengles_lite
 
 TARGET_A = $(TARGETPATH)/$(TARGETNAME).a
-TARGET_SO = $(TARGETPATH)/$(TARGETNAME).so
+TARGET_SO = $(TARGETPATH)/lib$(TARGETNAME).so
 
 DEPFILE= dependency_list_$(TARGETNAME)
 
 #-------------------------------------------------
 # 
 #-------------------------------------------------
-CROSS = /home/lfu/Desktop/scratchbox/compilers/arm-gcc4.1-uclibc20061004/bin/arm-linux-
+CROSS = arm-linux-
 CC = $(CROSS)g++
 AR = $(CROSS)ar rcv
-LD = $(CROSS)ld
+LD = $(CROSS)g++
 RM = rm -f 
 RANLIB = $(CROSS)ranlib
 MAKEDEP= $(CC) -M
@@ -319,7 +319,7 @@
 %.so:
 	@echo Build Shared Library
 	$(RM) $@
-	$(LD) -shared -soname $@.$(VER) -o $@.$(VER).0 $^
+	$(LD) -shared -o $@ $^
 
 include $(DEPFILE)
 
